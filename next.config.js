/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */

//module.exports = nextConfig;
const nextConfig = {
  reactStrictMode: true,
  basePath: '/',
  assetPrefix: '/',
  trailingSlash: true,
};

module.exports = nextConfig;